# Provider Logos

This folder is intentionally simple so logos can be swapped quickly later.

- `placeholder.svg` is currently used for all cloud providers.
- To customize branding, add provider-specific assets and update `CLOUD_PROVIDERS` in `src/renderer/App.tsx`.
